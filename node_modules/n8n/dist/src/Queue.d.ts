import Bull from 'bull';
import { IExecuteResponsePromiseData } from 'n8n-workflow';
export declare type Job = Bull.Job<JobData>;
export declare type JobQueue = Bull.Queue<JobData>;
export interface JobData {
    executionId: string;
    loadStaticData: boolean;
}
export interface JobResponse {
    success: boolean;
}
export interface WebhookResponse {
    executionId: string;
    response: IExecuteResponsePromiseData;
}
export declare class Queue {
    private activeExecutions;
    private jobQueue;
    constructor();
    add(jobData: JobData, jobOptions: object): Promise<Job>;
    getJob(jobId: Bull.JobId): Promise<Job | null>;
    getJobs(jobTypes: Bull.JobStatus[]): Promise<Job[]>;
    getBullObjectInstance(): JobQueue;
    stopJob(job: Job): Promise<boolean>;
}
export declare function getInstance(): Queue;
