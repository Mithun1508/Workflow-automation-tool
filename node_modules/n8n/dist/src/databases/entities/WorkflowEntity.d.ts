import type { IBinaryKeyData, IConnections, IDataObject, INode, IPairedItemData, IWorkflowSettings } from 'n8n-workflow';
import { TagEntity } from './TagEntity';
import { SharedWorkflow } from './SharedWorkflow';
import { AbstractEntity } from './AbstractEntity';
import type { IWorkflowDb } from '../../Interfaces';
export declare class WorkflowEntity extends AbstractEntity implements IWorkflowDb {
    id: number;
    name: string;
    active: boolean;
    nodes: INode[];
    connections: IConnections;
    settings?: IWorkflowSettings;
    staticData?: IDataObject;
    tags?: TagEntity[];
    shared: SharedWorkflow[];
    pinData: ISimplifiedPinData;
    hash: string;
    setHash(): void;
}
export interface ISimplifiedPinData {
    [nodeName: string]: Array<{
        json: IDataObject;
        binary?: IBinaryKeyData;
        pairedItem?: IPairedItemData | IPairedItemData[] | number;
    }>;
}
