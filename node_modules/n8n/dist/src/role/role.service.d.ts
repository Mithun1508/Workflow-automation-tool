import { EntityManager } from 'typeorm';
import { Role } from '../databases/entities/Role';
export declare class RoleService {
    static get(role: Partial<Role>): Promise<Role | undefined>;
    static trxGet(transaction: EntityManager, role: Partial<Role>): Promise<Partial<Role> | undefined>;
}
