import { SharedWorkflow } from '../databases/entities/SharedWorkflow';
import { User } from '../databases/entities/User';
import { WorkflowEntity } from '../databases/entities/WorkflowEntity';
export declare class WorkflowsService {
    static getSharing(user: User, workflowId: number | string, relations?: string[], { allowGlobalOwner }?: {
        allowGlobalOwner: boolean;
    }): Promise<SharedWorkflow | undefined>;
    static get(workflow: Partial<WorkflowEntity>, options?: {
        relations: string[];
    }): Promise<WorkflowEntity | undefined>;
    static updateWorkflow(user: User, workflow: WorkflowEntity, workflowId: string, tags?: string[], forceSave?: boolean): Promise<WorkflowEntity>;
}
